# ===========================================
# MiroFish TH (DEEDY) — เครื่องยนต์จำลองสังคม backend/core/
# ===========================================
# ลงเพิ่มจาก requirements.txt:  pip install -r requirements-core.txt
# ===========================================

# ============= LLM =============
# ใช้ openai SDK ตรงๆ ไม่ใช้ LangChain (ดู D2 ใน PLAN.md)
# ต้องล็อก <2 เพราะ camel-ai ต้องการ openai<2 และ langchain-openai
# ต้องการ >=2.45 ซึ่งอยู่ร่วม venv เดียวกันไม่ได้
openai>=1.86.0,<2

# ============= Memory / RAG =============
chromadb>=1.0.0
# embedding แบบ multilingual รันในเครื่อง (ดู D4/D5 ใน PLAN.md)
sentence-transformers>=3.0.0

# ============= เครือข่ายสังคม =============
networkx>=3.0

# ============= API =============
fastapi>=0.110.0
uvicorn>=0.27.0

# ============= ภาษาไทย =============
pythainlp>=4.0.0

# ============= อื่นๆ =============
python-dotenv>=1.0.0
